apiVersion: v1
kind: ConfigMap
metadata: { name: prometheus-config }
data:
  prometheus.yml: |
    scrape_configs:
      - job_name: freeai
        static_configs: [{ targets: ["freeai-router:8010"] }]
        metrics_path: /metrics
